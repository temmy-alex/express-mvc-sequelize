<%- include('../includes/head.ejs') %>
<div class="container">
    <div class="row">
        <% if(family.length > 0) { %>
            <% for (let families of family) { %>
                <div class="col-md-4">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title"><%= families.nama %></h5>
                            <p class="card-text"><%= families.umur %></p>
                            <p class="card-text"><%= families.status %></p>
                            <a href="#" class="btn btn-primary">Go Go Go!!!</a>
                        </div>
                    </div>
                </div>
            <% } %>
        <% }else{ %>
            <h1>No Category Found!</h1>
        <% } %>
        
    </div>
</div>
<%- include('../includes/end.ejs') %>
